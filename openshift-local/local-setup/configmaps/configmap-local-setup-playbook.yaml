apiVersion: v1
kind: ConfigMap
metadata:
  name: local-setup-playbook
  namespace: coralberryfairy
data:
  local-setup.yaml: |
    ---
    - name: local-setup
      hosts: localhost
      connection: local
      gather_facts: false
      roles:
        - local-setup
